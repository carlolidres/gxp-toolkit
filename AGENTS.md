# AGENTS.md

**Start here.** This project uses the **GxP Toolkit** AI-agent workflow.

Full instructions live in `agent-workflow/`. Read in this order:

```text
agent-workflow/AGENTS.md
↓
agent-workflow/DOX.md
↓
agent-workflow/HANDOFF.md
↓
agent-workflow/PLAN.md
↓
baseline-.md
↓
.cursor/rules/
↓
graphify-out/ (codebase map)
↓
sqlite-out/ (schema map)
```

**Project owners:** define what you are building in **`baseline-.md` only**. Agents read it for project-specific goals, roles, and schema intent.

Follow the instructions in `agent-workflow/AGENTS.md`.
