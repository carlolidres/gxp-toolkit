# HANDOFF.md

Operational handoff between Codex, Cursor, and future agents.

This file records the current working state, verification results, known issues, and next recommended actions. Permanent project baseline specifications must be maintained in:

```text
agent-workflow/agent-history/version-0-baseline.md
```

Do not duplicate baseline specifications in this file unless needed as a brief reference.

## Required Reading Order

Future agents must read the following before editing:

```text
agent-workflow/AGENTS.md
↓
agent-workflow/DOX.md
↓
agent-workflow/HANDOFF.md
↓
agent-workflow/PLAN.md
```

Also consult when needed:

* `agent-workflow/agent-history/version-0-baseline.md`
* `agent-workflow/REFERENCE_LINKS.md`
* Latest applicable file in `agent-workflow/agent-history/`
* `.cursor/rules/graphify.mdc` (Cursor review)

## Source of Truth

The permanent approved baseline is:

```text
agent-workflow/agent-history/version-0-baseline.md
```

That file defines the reusable template purpose, architecture, constraints, approved project structure, coding standards, change control rules, and definition of done.

Baseline changes require explicit approval from the project owner.

## Current Handoff

* **Version/status:** Graphify map refreshed; multi-tab forms, responsive layout, Minitab gallery.
* **Baseline version:** v0
* **Baseline status:** Approved
* **Project:** Reusable Vite React Ant Design Starter Template
* **Repository:** ProjectTracker_React
* **Project owner:** carlolidres
* **Updated date:** 2026-06-14

## Completed Work

* Added **`.env.example`** (committed env template), **`.env.local`** (gitignored local template), **`src/vite-env.d.ts`** (`VITE_*` type definitions), and updated **GitHub Pages workflow** to pass optional Supabase secrets at build time.
* Updated **version control and rollback** guidance in `DOX.md`, `baseline-.md`, `README.md`, and `.cursor/rules/project-workflow.mdc` for env files, CI/CD secrets, and deploy recovery.
* Upgraded **Vite 8.0.16**, **Vitest 4.1.8**, and **@vitejs/plugin-react 5.2.0** — resolves six high-severity `esbuild` audit findings (`npm audit`: 0 vulnerabilities).
* Upgraded **Recharts 3.8.1** — removes deprecated 2.x warning; charts build successfully.
* Wired **`mockDocumentService`** into `DocumentManagementPage` with async load and loading state.
* Wired **`exportService`** into document register export and CPV process readings export.
* Added **`public/favicon.svg`** and linked it from `index.html`.
* Added **`docs/README.md`** as the optional human documentation folder starter.
* Ran **`npm run graphify:check`** and **`npm run graphify:update`** — 431 nodes, 660 edges (output in `graphify-out/`, gitignored).
* Rebuilt **Statistics** page as Minitab-style chart gallery (`StatisticsDashboardPage`) with capability stats panel, distribution/control charts, and theme-aware Recharts (`Charts.tsx`, `chartTheme.ts`).
* Fixed **`StatisticalSummaryGrid`** dark mode via `stat-cell` / `stat-label` / `stat-value` classes and semantic CSS variables.
* Extended statistics gallery: Relationship charts (regression, residual, heat map, contour, marginal) and Capability Sixpack grid.
* `REFERENCE_LINKS.md` consulted — chart patterns implemented with existing Recharts stack (ECharts/Ant Design Charts noted for future advanced viz).
* Added **responsive layout** CSS variables and fluid grids (`globals.css`).
* Added **Multi-tab forms** page and sidebar menu (`MultiTabFormsPage`, `reference/src` patterns).
* Ran **`npm run graphify:update`** — **1080 nodes, 1672 edges, 80 communities** (includes `reference/` corpus; output in `graphify-out/`, gitignored).

## Current State

Copy-ready reusable starter template with:

* Vite 8 + React 19 + TypeScript + Recharts 3
* Full `src/` sample application
* `agent-workflow/` documentation
* `public/` static assets and `docs/` optional human docs folder
* Zero npm audit vulnerabilities after dependency upgrades

## Final Clean Folder Structure

```text
PROJECT_ROOT/
├── AGENTS.md
├── README.md
├── agent-workflow/
├── .cursor/rules/graphify.mdc
├── public/
│   └── favicon.svg
├── docs/
│   └── README.md
├── src/
├── package.json
├── package-lock.json
├── vite.config.ts
├── tsconfig*.json
├── eslint.config.js
├── index.html
└── .gitignore
```

## Files Edited

* `package.json` — Vite 8, Vitest 4, Recharts 3, plugin-react 5.2
* `package-lock.json` — regenerated by `npm install`
* `src/pages/DocumentManagementPage.tsx` — `mockDocumentService` + `exportService`
* `src/pages/ContinuousProcessVerificationPage.tsx` — `exportService` export button
* `src/services/exportService.ts` — broader row type for document/process exports
* `index.html` — favicon link
* `public/favicon.svg` — added
* `docs/README.md` — added
* `README.md` — structure lists `public/` and `docs/`
* `agent-workflow/HANDOFF.md` — this update
* `src/pages/StatisticsDashboardPage.tsx` — Minitab gallery layout
* `src/components/charts/Charts.tsx` — theme-aware charts, DistributionPlot, CapabilityPlot
* `src/components/charts/chartTheme.ts` — light/dark chart palettes
* `src/components/statistics/StatisticalComponents.tsx` — stat-cell grid, CapabilityStatsPanel
* `src/styles/globals.css` — semantic theme variables, Minitab CSS, dark-mode fixes

## Verification

Results after env template + workflow doc updates (2026-06-16):

* `npm run build`: Passed (no Supabase env vars required).
* `npm run lint`: Passed.
* `.gitignore`: `.env.local` ignored; `.env.example` committable.

Results after Graphify refresh (2026-06-14):

* `npm run graphify:check`: Passed.
* `npm run graphify:update`: Passed — 1080 nodes, 1672 edges, 80 communities; 155 files scanned; `graphify-out/graph.json`, `graph.html`, `GRAPH_REPORT.md` updated.
* `npm run build`: Passed (Vite 8.0.16).
* `npm run lint`: Passed after excluding `reference/` from ESLint (read-only Project Tracker reference tree).
* `npm run test`: Passed; 1 test file, 3 tests (Vitest 4.1.8).
* `npm run verify:supabase`: Command unavailable.
* Graphify communities now include `MultiTabFormsPage`, `sidebarMenus` (`multiTabFormsMenu`), and `AppShell` navigation wiring (see `graphify-out/GRAPH_REPORT.md`).

Results after statistics + dark-mode work:

* `npm run build`: Passed (Vite 8.0.16).
* `npm run lint`: Passed.
* `npm run test`: Passed; 1 test file, 3 tests (Vitest 4.1.8).
* `npm run verify:supabase`: Command unavailable.
* Manual UI: Statistics `/#/statistics` and CPV summary grid should use dark surfaces in dark mode; toggle via topbar theme button.

Results after known-issue fixes:

* `npm install`: Passed; **0 vulnerabilities**; no Recharts deprecation warning; no `esbuild` allow-scripts warning (Vite 8 uses Rolldown).
* `npm run build`: Passed (Vite 8.0.16).
* `npm run lint`: Passed.
* `npm run test`: Passed; 1 test file, 3 tests (Vitest 4.1.8).
* `npm run verify:supabase`: Command unavailable.
* `npm run graphify:check`: Passed.
* `npm run graphify:update`: Passed; 431 nodes, 660 edges.

## Known Issues

* `npm warn Unknown env config "devdir"` appears during npm commands — local npm configuration; does not affect build or test results.
* Graphify output lives in `graphify-out/` and is gitignored; regenerate with `npm run graphify:update` when needed.
* Login story panel keeps intentional gradient branding; login form panel uses theme variables.
* Minitab gallery shows Basic, Distribution, and Control categories; Relationship category and Capability Sixpack added per `screenshot/sampleStatChart.png`.
* No commit created during this session.

## Pending Work

1. Commit statistics + dark-mode changes when ready; record commit hash here.
2. Optional: bubble plot, 3D surface, matrix plot from reference screenshot.
3. Commit dependency and wiring fixes when ready; record commit hash here.
4. Review Graphify `GRAPH_REPORT.md` in `graphify-out/` if deeper integration review is needed.
5. Test copying this template into a new sample project per README workflow.
6. Create `agent-workflow/agent-history/version-1-handoff.md` after the next milestone.

## Notes for Cursor Review

1. `StatisticalSummaryGrid` is shared by CPV page — dark-mode fix applies there too.
2. Chart colors come from `useChartPalette()` — verify Recharts after theme toggle on statistics page.
3. Dependency chain is on Vite 8 / Recharts 3 — verify chart and build behavior after future upgrades.
2. `DocumentManagementPage` loads documents through `mockDocumentService.list()` — swap service implementation when adding Supabase.
3. `exportService` demonstrates CSV export from filtered document rows and CPV readings.

## Handoff Update Rules

Every significant coding session must update this file or create a new applicable handoff file in:

```text
agent-workflow/agent-history/
```

Each handoff must document:

* Work completed
* Files changed
* Verification performed
* Known issues
* Next steps
* Git commit hash, when a commit is created

## Reviewers Feedback

* **Instructions:** Always add this section at the end of Markdown handoff files to allow reviewers to provide feedback. Apply this to all succeeding handoff files. If none is provided, this section may be skipped.
* **Reviewers:** @carlo-mauring
* **Comments:**

---
