export const MOCK_MUST_CHANGE_PASSWORD_KEY = 'gxp-toolkit-must-change-password'

/** Mock-mode only; Supabase uses DEFAULT_RESET_PASSWORD secret on the edge function. */
export const MOCK_DEFAULT_RESET_PASSWORD = 'iLoveJesus'
