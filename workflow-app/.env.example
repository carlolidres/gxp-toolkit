# Copy this file to workflow-app/.env and fill in your real value.
# The workflow app reads OPENAI_API_KEY from the environment (or this .env)
# on the backend only. Never commit the real .env file.

OPENAI_API_KEY=sk-your-openai-api-key-here
